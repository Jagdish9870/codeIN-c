#include<iostream>
#include<string.h>
using namespace std;
int main()
{
   char str[]="jack varshney";
   cout<<str<<endl;
   int s=strlen(str);      //strlen() is used to count the length of string,strrev() is used to reverse the string
   cout<<s;               //strcat() is used to add strings,  strcpy() is used to copy one string to another
}
